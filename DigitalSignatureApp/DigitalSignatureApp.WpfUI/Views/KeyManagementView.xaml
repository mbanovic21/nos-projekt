<Window x:Class="DigitalSignatureApp.WpfUI.Views.KeyManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DigitalSignatureApp.WpfUI.ViewModels"
        mc:Ignorable="d"
        Title="Upravljanje Kljucevima"
        Height="320" Width="480"
        ResizeMode="NoResize">

    <Window.DataContext>
        <vm:KeyManagementViewModel/>
    </Window.DataContext>

    <StackPanel Margin="20" Orientation="Vertical" VerticalAlignment="Top" >
        <TextBlock Text="Putanja do javnog ključa:"/>
        <TextBox Text="{Binding PublicKeyPath}" Margin="0,5,0,10"/>

        <TextBlock Text="Putanja do privatnog ključa:"/>
        <TextBox Text="{Binding PrivateKeyPath}" Margin="0,5,0,10"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Generiraj ključeve" Width="140" Command="{Binding GenerateKeysCommand}" Margin="5,0"/>
            <Button Content="Spremi ključeve" Width="120" Command="{Binding SaveKeysCommand}" Margin="5,0"/>
            <Button Content="Učitaj ključeve" Width="120" Command="{Binding LoadKeysCommand}" Margin="5,0"/>
        </StackPanel>

        <TextBlock Text="{Binding StatusMessage}" 
               Foreground="SteelBlue" 
               TextWrapping="Wrap" 
               Margin="0,15,0,0"/>
    </StackPanel>
</Window>
