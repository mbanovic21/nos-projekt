<UserControl x:Class="DigitalSignatureApp.WpfUI.Views.AesEncryptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DigitalSignatureApp.WpfUI.ViewModels"
             Height="Auto" Width="Auto">
    <UserControl.DataContext>
        <vm:AesEncryptionViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1.2*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,12,0">
            <TextBlock Text="🔒 AES Enkripcija" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

            <GroupBox Header="AES ključ i IV" Margin="0,0,0,10">
                <StackPanel>
                    <WrapPanel Margin="0,4">
                        <Button Content="Generiraj ključ" Command="{Binding GenerateKeyCommand}" Margin="0,0,8,0"/>
                        <Button Content="Spremi ključ (AppData)" Command="{Binding SaveKeyFilesCommand}" Margin="0,0,8,0"/>
                        <Button Content="Učitaj ključ (AppData)" Command="{Binding LoadKeyFilesCommand}"/>
                    </WrapPanel>

                    <CheckBox Content="Prikaži u hex" IsChecked="{Binding ShowHex}" Margin="0,8,0,6"/>

                    <TextBlock Text="Key (Base64):" FontWeight="SemiBold" />
                    <TextBox Text="{Binding AesKeyBase64}" Height="48" TextWrapping="Wrap" IsReadOnly="True" Visibility="{Binding ShowHex, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=False}" />

                    <TextBlock Text="IV (Base64):" FontWeight="SemiBold" Margin="0,6,0,0"/>
                    <TextBox Text="{Binding AesIvBase64}" Height="36" TextWrapping="Wrap" IsReadOnly="True" Visibility="{Binding ShowHex, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=False}" />

                    <TextBlock Text="Key (Hex):" FontWeight="SemiBold" Margin="0,6,0,0"/>
                    <TextBox Text="{Binding AesKeyHex}" Height="48" TextWrapping="Wrap" IsReadOnly="True" Visibility="{Binding ShowHex, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}" />

                    <TextBlock Text="IV (Hex):" FontWeight="SemiBold" Margin="0,6,0,0"/>
                    <TextBox Text="{Binding AesIvHex}" Height="36" TextWrapping="Wrap" IsReadOnly="True" Visibility="{Binding ShowHex, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Datoteke" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="Ulazna datoteka" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,6">
                        <TextBox Text="{Binding InputFilePath}" Width="420" IsReadOnly="True"/>
                        <Button Content="Odaberi" Command="{Binding SelectInputFileCommand}" Margin="8,0,0,0"/>
                    </StackPanel>

                    <TextBlock Text="Izlazna datoteka (opcionalno)" FontWeight="SemiBold" Margin="0,8,0,0"/>
                    <StackPanel Orientation="Horizontal" Margin="0,6">
                        <TextBox Text="{Binding OutputFilePath}" Width="420" IsReadOnly="True"/>
                        <Button Content="Postavi" Command="{Binding SelectOutputFileCommand}" Margin="8,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,8,0,0">
                <Button Content="Enkriptiraj" Command="{Binding EncryptFileCommand}" Width="130" Margin="0,0,8,0"/>
                <Button Content="Dekriptiraj" Command="{Binding DecryptFileCommand}" Width="130"/>
            </StackPanel>

            <TextBlock Text="Status:" FontWeight="SemiBold" Margin="0,12,0,4"/>
            <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="DarkBlue"/>

        </StackPanel>

        <StackPanel Grid.Column="1">
            <TextBlock Text="Detaljan pregled (log)" FontWeight="Bold" Margin="0,0,0,8"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="480">
                <TextBox Text="{Binding Log}" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
            </ScrollViewer>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="Kopiraj Key (Base64)" Margin="4"/>
                <Button Content="Kopiraj IV (Base64)" Margin="4"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
