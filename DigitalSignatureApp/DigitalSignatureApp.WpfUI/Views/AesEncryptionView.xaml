<Window x:Class="DigitalSignatureApp.WpfUI.Views.AesEncryptionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DigitalSignatureApp.WpfUI.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="AES Encryption / Decryption" Height="320" Width="520">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="AES Enkripcija i Dekripcija" FontWeight="Bold" FontSize="18" Margin="0,0,0,15" Grid.Row="0"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5">
            <TextBox Width="340" Text="{Binding InputFilePath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" />
            <Button Content="Odaberi ulaznu datoteku" Command="{Binding SelectInputFileCommand}" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,5">
            <TextBox Width="340" Text="{Binding OutputFilePath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" />
            <Button Content="Odaberi izlaznu datoteku" Command="{Binding SelectOutputFileCommand}" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Generiraj Ključ" Command="{Binding GenerateKeyCommand}" Width="140" Margin="5"/>
            <Button Content="Enkriptiraj" Command="{Binding EncryptCommand}" Width="120" Margin="5"/>
            <Button Content="Dekriptiraj" Command="{Binding DecryptCommand}" Width="120" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
