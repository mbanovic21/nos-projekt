<UserControl x:Class="DigitalSignatureApp.WpfUI.Views.SignatureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Naslov -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="🖋️ Digitalni potpis (prezentacijski prikaz)"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

        <!-- Glavni panel -->
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Vertical" Margin="0,0,10,0">
            <TextBlock Text="Odabrana datoteka:" FontWeight="SemiBold" Margin="0,0,0,3"/>
            <TextBox Text="{Binding InputFilePath}" IsReadOnly="True" TextWrapping="Wrap"/>
            <Button Content="Odaberi datoteku" Command="{Binding SelectInputFileCommand}" Width="150" Height="35" Margin="0,5,0,12"/>

            <TextBlock Text="Digitalni potpis:" FontWeight="SemiBold" Margin="0,0,0,3"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox Text="{Binding SignatureValue}" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
            </ScrollViewer>
        </StackPanel>

        <!-- Log panel -->
        <GroupBox Header="Log" FontSize="14" Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox Text="{Binding Log}" IsReadOnly="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
            </ScrollViewer>
        </GroupBox>

        <!-- Footer buttons -->
        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Potpiši datoteku" Command="{Binding SignFileCommand}" Width="150" Height="35" Margin="5"/>
            <Button Content="Provjeri potpis" Command="{Binding VerifySignatureCommand}" Width="150" Height="35" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
